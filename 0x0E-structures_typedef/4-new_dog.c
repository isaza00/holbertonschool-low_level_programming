#include <stdlib.h>
#include "dog.h"
/**
 * init_dog - isdigit
 * @d: integer
 * @name: integer
 * @age: integer
 * @owner: integer
 * Return: prints alphabet
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	if (d == NULL)
	{
		d = malloc(sizeof(struct dog));
		if (d == NULL)
			return;
	}
	d->name = name;
	d->age = age;
	d->owner = owner;
}